// Shared constants — no server imports, safe to import from client components.
export const PRELOADER_DEFAULTS = {
  images:      ["/logonc.svg"],
  animation:   "bounce",
  minDuration: 1200,
  text:        "Chargement…",
  showText:    true,
};
